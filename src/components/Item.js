import { Link, useParams } from "react-router-dom";
import React from "react";

function Item(props) {
  console.log(props);
  // if (props.wanted === "true") {
  return (
    <></>)}

export default Item;
